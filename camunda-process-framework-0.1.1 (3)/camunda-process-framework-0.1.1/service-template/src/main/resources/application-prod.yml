# Production profile: minimal logging, minimal actuator exposure, no SQL echo.
logging:
  level:
    root: WARN
    com.aaseya: INFO

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus

spring:
  jpa:
    show-sql: false
