# Development profile: verbose logging, all actuator endpoints, SQL echo.
logging:
  level:
    root: INFO
    com.aaseya: DEBUG
    org.springframework.security: DEBUG

management:
  endpoints:
    web:
      exposure:
        include: "*"

spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
